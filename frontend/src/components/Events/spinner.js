import React from 'react';
import './spinner.css'; // Include CSS for styling the spinner

const Spinner = () => {
  return (
    <div className="spinner-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;
